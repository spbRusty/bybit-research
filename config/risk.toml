# Risk configuration for paper trading
equity_start = 1_000.0
risk_per_trade_pct = 0.01
stop_k_vol = 2.0
take_k_stop = 1.5
slippage_bps = 2.0
fee_round_trip = 0.0015

# Position limits
max_position_size_usd = 100.0
max_exposure_usd = 500.0
max_open_positions = 5

# Default instrument parameters (overridden by BybitInstrumentInfo)
instrument_qty_step = 0.001
instrument_min_lot = 0.001
instrument_max_lot = 1000.0
instrument_min_notional = 5.0
instrument_tick_size = 0.00001
