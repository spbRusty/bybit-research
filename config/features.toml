# Признаки и предварительный фильтр свечей (ТЗ §16, §24)
prefilter_rel_volume = 3.0   # RVol: объём свечи / медиана окна (порог X — кандидат на исследование)
prefilter_rel_range = 3.0    # RRange: (high-low)/close / медиана окна (порог Y — кандидат на исследование)

vol_window_min = 60          # окно признаков волатильности, минут
vol_window_days = 30         # окно Гармана–Класса, дней

# Интервалы будущих доходностей (ТЗ §28), минут
future_horizons_min = [1, 3, 5, 10, 15, 30]

# BTC — фактор рынка (ТЗ §25): только свечные признаки на первом этапе
btc_symbol = "BTCUSDT"
btc_vol_window_min = 60